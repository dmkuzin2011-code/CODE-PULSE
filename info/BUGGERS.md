# Fixed Bugs

## Beta 0.0.2

1. **Repeating code block in `cmd_export` function** — fixed duplicate code that was causing redundant operations
2. **Error handlers improvements** — fixed safeguard in `HistoryResult` class for `history_delete` command
3. **Command simplification** — changed "version control" command to simplified "version"

---

*Last updated: Beta 0.0.2*